<?xml version="1.0" encoding="utf-8"?>
<!-- Liquid Glass download row -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@drawable/menu_item_background"
    android:padding="14dp"
    android:layout_marginBottom="7dp"
    android:gravity="center_vertical"
    android:clickable="false"
    android:focusable="false">

    <!-- Status icon (downloading arrow or checkmark) -->
    <ImageView
        android:id="@+id/download_icon"
        android:layout_width="34dp"
        android:layout_height="34dp"
        android:src="@drawable/ic_download"
        android:scaleType="centerInside"
        android:layout_marginEnd="12dp" />

    <!-- Title + progress + status text -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/download_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/text_primary"
            android:textSize="14sp"
            android:fontFamily="sans-serif-medium"
            android:maxLines="1"
            android:ellipsize="end" />

        <ProgressBar
            android:id="@+id/download_progress"
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:layout_marginTop="5dp"
            android:layout_marginBottom="3dp"
            android:progressDrawable="@drawable/progress_bar_drawable"
            android:max="100"
            android:progress="0"
            android:visibility="gone" />

        <TextView
            android:id="@+id/download_status"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/text_secondary"
            android:textSize="12sp"
            android:layout_marginTop="2dp" />
    </LinearLayout>

    <!-- Open file button (shown when complete) -->
    <ImageView
        android:id="@+id/btn_open_download"
        android:layout_width="34dp"
        android:layout_height="34dp"
        android:src="@drawable/ic_open_file"
        android:background="@drawable/button_background"
        android:padding="6dp"
        android:scaleType="centerInside"
        android:visibility="gone"
        android:contentDescription="Open file"
        android:layout_marginStart="6dp" />

    <!-- Delete button -->
    <ImageView
        android:id="@+id/btn_delete_download"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@drawable/ic_delete"
        android:background="@drawable/button_background"
        android:padding="5dp"
        android:scaleType="centerInside"
        android:alpha="0.5"
        android:contentDescription="Remove"
        android:layout_marginStart="6dp" />

</LinearLayout>
